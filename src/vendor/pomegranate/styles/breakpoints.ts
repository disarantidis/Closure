/*
  GENERATED by design-tokens/generate-css.mjs from the foundation's breakpoint sets.
  DO NOT EDIT — run `npm run tokens`; tokens-drift-test pins this file to the JSON.

  The ladder RESPONSIVE-RULES reads: the page reflows by these rungs (mobile first,
  min-width only), a region by its own container. Read a rung by name — `BREAKPOINTS.laptop`
  — never retype the number.
*/
export const BREAKPOINTS = {
  "mobile": 0,
  "tablet": 768,
  "laptop": 1024,
  "desktop": 1440,
  "large-desktop": 1920
} as const

export type Breakpoint = keyof typeof BREAKPOINTS
